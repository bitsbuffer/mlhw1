The code is available on https://github.com/bitsbuffer/mlhw1
It has 12 notebooks, essentially different notebooks for each of the 5 algorithms on 2 seperate datasets and 2 notebooks for Auto EDA.
it has 2 html files which Auto EDA output for both datasets.

The environment.yml file can help in the setup of environment.
The neural networks are run on Google colab with default environments where fastai library version is 1.0.61.